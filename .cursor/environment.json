{
  "name": "poppin-music",
  "install": "./scripts/setup.sh",
  "terminals": [
    {
      "name": "convex",
      "command": "CONVEX_AGENT_MODE=anonymous npx convex dev",
      "description": "Local Convex backend (anonymous agent mode) on port 3210. Watches convex/ and pushes functions + regenerates types on change."
    },
    {
      "name": "vite",
      "command": "npm run dev",
      "description": "Vite dev server for the React frontend on http://localhost:5173."
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "convex-backend", "port": 3210 }
  ]
}
